{% extends 'base.html.twig' %}

{% block title %}Hello DefaultController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }

</style>
    <div class="div" style="display: flex; justify-content: center;">
        {% for product in product %}
                <div class="col-xl-4 col-md-6 mb-4" style="text-decoration: none;">
                    <div class="card border-0 shadow">
                        <a href="/book/{{ product.id }}">
                            <div class="card-body text-center" style="padding: 0 1.25rem 1.25rem">
                                <h5 class="card-title mb-0" style="font-size: 25px">{{ product.naam }}</h5>
                                <div class="card-text text-black-50">€{{ product.prijs }}</div>
                            </div>
                        </a>
                    </div>
                </div>
        {% endfor %}
    </div>
{% endblock %}
